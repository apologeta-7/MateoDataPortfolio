# Notebooks

The notebooks are analysis artifacts from the project buildout. The final
reproducible serving path is implemented in `src/`, `api/`, `tests/` and the
saved model/report artifacts.

Notebook 09 was replaced by the productionized `src` inference modules and API
tests. It is kept as a lightweight handoff notebook explaining that refactor.

